//
// Created by 30980 on 2025/12/21.
//

#include <iostream>

#include "amemory/detail/mem/vm_readwrite.h"
// #include "amemory/detail/search/basic_search.hpp"

#include "amemory/maps.h"
#include "amemory/detail/mem/p_readwrite.h"

namespace memory_test
{
    void test_search(pid_t pid)
    {
        // amemory::detail::basic_search<amemory::detail::p_readwrite> search(pid);
        // int value = 1;
        // amemory::detail::search_result<int> result;
        // amemory::maps maps(pid);
        // maps.load();
        // search.set_ranges(static_cast<int>(amemory::memory_t::C_ALLOC));
        // auto id = search.search(value, maps.begin(), maps.end(), result);
        // search.wait(id);
    }
}
